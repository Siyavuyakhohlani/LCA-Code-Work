let colors = [];
colors.push("blue", "red", "yellow");

// console.log(colors);

let numbers = [1, 2, 3, 4, 5]

// remove the last item from the colors array
colors.pop();
// console.log(colors);

// add another color from the beginning of the colors array
colors.unshift("gold");
// console.log(colors);

//print length of both array
console.log(colors.length);
console.log(numbers.length);
